# Changelog

## v1.1.0

### Features

- Support for dynamic Excel column definition through a function parameter.

  The `excelColumns` parameter now accepts either an array of column definitions or a function that receives the first data row and returns column definitions.
